export interface TBuildInfo {
  currentTimeStr: string;
  currentTimeTag: string;
  gitBranch: string;
  gitCommitHash: string;
  projectName: string;
  timestamp: string;
  timetag: string;
  version: string;
}
